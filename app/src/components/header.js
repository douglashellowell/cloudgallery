import React from 'react';
import styles from './header.module.sass'

const Header = () => {
    return (
        <header>
            <h1>cloudGallery</h1>
        </header>
    );
};

export default Header;